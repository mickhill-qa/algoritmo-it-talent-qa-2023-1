algoritmo "somaparametroprocedimento"

var
x,y: inteiro

Procedimento Soma(a,b:inteiro)

inicio
    a <- a + 1
    b <- b+ 2
    escreval("Recebi o valor", a)
    escreval("Recebi o valor", b)
    escreval("A soma entre os dois e ", a+b)
FimProcedimento

inicio
    x<- 4
    y<- 8
    Soma(x,y)
    escreval("Valor de X = ", x)
    escreval("Valor de Y = ", y)
fimalgoritmo