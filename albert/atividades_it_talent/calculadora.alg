algoritmo "calculadora"

var
    x : inteiro
    y : inteiro

inicio
    escreval("Primeiro termo")
    leia(x)
    escreval("Segundo termo")
    leia(y)
    escreva(x, " + ", y, " = " , x+y)

fimalgoritmo