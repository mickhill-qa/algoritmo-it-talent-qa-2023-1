algoritmo "valores"

var

    n1,n2: inteiro
    M: real
inicio
    escreval("Informe um numero")
    leia(n1)
    escreval("Informe outro nome")
    leia(n2)
    M <- (n1 + n2)/2
    escreval("A media entre ", n1, " e ",n2, " e igual a ",M )

fimalgoritmo