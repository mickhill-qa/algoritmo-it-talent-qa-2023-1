algoritmo "PRÁTICA 01"

var
    x,y : inteiro
    

inicio
    escreval("PRIMEIRO NÚMERO")
    leia(x)
    escreval("SEGUNDO NÚMERO")
    leia(y)
    escreval(x, " + ", y, " = " , x+y)
    escreval("A MEDIA É ",(x+y)/2)

fimalgoritmo