algoritmo "CONTEATE0"
 
var

    contado : inteiro

inicio

    contado <- 10
    enquanto (contado >= 0) faca
        escreval(contado)
        contado <- contado -1
    fimenquanto
    escreval("Terminei de contar")
fimalgoritmo